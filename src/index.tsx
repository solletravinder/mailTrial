import * as React from "react";
import { render } from "react-dom";
import PrimarySearchAppBar from "./PrimarySearchAppBar";
const rootElement = document.getElementById("root");
render(<PrimarySearchAppBar />, rootElement);
